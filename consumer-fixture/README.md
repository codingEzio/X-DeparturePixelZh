# Sample consumer fixture

This neutral fixture proves that a consumer receives font binaries, required notices, and public provenance in one guarded synchronization. Its generated assets and receipt are ignored. It does not represent a shipped application.
